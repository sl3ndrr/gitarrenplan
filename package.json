{
  "name": "gitarrenplan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:browser": "playwright test",
    "test:all": "npm run test:unit && npm run test:browser"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.62.0",
    "vitest": "^3.2.4"
  }
}
